return {
	Colors = {
		Background = Color3.fromRGB(255, 211, 49), --// Background Color of UI
		Accent = Color3.fromRGB(56, 56, 56), --// Accent Color of UI
		PrimaryText = Color3.fromRGB(25, 25, 25), --// Primary Text Color
		SuggestionText = Color3.fromRGB(95, 95, 95), --// Suggestion Text Color
		Divider = Color3.fromRGB(93, 93, 93),
	},
	Options = {
		Font = Enum.Font.SciFi, --// UI Font
		CornerRadius = UDim.new(0, 6), --// Corner Radius of UI
		BackgroundOpacity = 0.25, --// Background Opacity of UI
	}
}
